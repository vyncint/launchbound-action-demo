# The known-stable counterpart to reduce-flip: same reduction, every barrier
# unconditional. The gate must disqualify NOTHING at any block size.
[kernel]
name = "reduce-stable-demo"
entry = "reduce"
domain = 1
needs_cc = "8.0"
known = "stable"

[dims.block_x]
values = [32, 64]

[dims.tile]
role = "spec"
values = [128, 256]

[constraints]
exprs = ["tile % block_x == 0"]

[bench]
elements = 1048576
grid_x = "elements / block_x"

# PTX ABI (vecadd-verified lowering): &[f32] -> ptr,len; DisjointSlice<f32>
# -> ptr,len. Four .param slots; `stage` validates against the real PTX.
[[bench.args]]
kind = "in_f32"
len = "elements"
[[bench.args]]
kind = "len_of"
of = 0
[[bench.args]]
kind = "out_f32"
len = "elements"
[[bench.args]]
kind = "len_of"
of = 2

[model]
# f32 tile in shared memory.
smem_bytes = "tile * 4"
