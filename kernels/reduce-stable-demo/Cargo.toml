[package]
name = "reduce-stable-demo"
version = "0.1.0"
edition = "2024"
license = "MIT OR Apache-2.0"
publish = false

# Standalone fixture: not a workspace member. Depends on the sibling
# cuda-oxide checkout (see corpus/README.md for the required layout).
[workspace]

[dependencies]
cuda-device = { path = "../../../cuda-oxide/crates/cuda-device" }
